# PROGRESS — PrintLapse（拓竹 A1 打印延时视频生成器）

## 开工回执（2026-09-14）
- 目标：拖入切好片的 .gcode.3mf/.gcode → 在代码画的 A1 里逐层长出模型 → 导出尺寸、帧数分毫不差的 MP4/WebM。
- 顺序：0 样本 → 1 解析+导出+verify.js（兜底）→ 2 逐层生长与换色 → 3 A1 场景/三机位/界面 + 冻结判据 → 4 上线。
- 最大风险：tower 样本 28.7 万挤出段 + 3.8 万圆弧，打开和逐帧渲染的速度；无头 Chrome 里 WebGL 读回像素慢。
- 次风险：视频画面是否真的"跟 G-code 一致"只能靠像素判，判据要设计成坏了一定变红。

## 任务 0 复核（全部实跑）
- `python tools/make_fixtures.py` 16 秒切完三个样本：
  cube 14997 行/100 层；bearing4c 7.03MB/283083 行/100 层；tower 11.80MB/435694 行/750 层
- bearing4c：挤出 G1 128226、圆弧 G2/G3 30644、`M620 S` 123 条（S1A 59/S0A 33/S2A 30/S255 1）、热床 256×256、颜色 #FF0000;#0055FF;#FFFFFF;#222222 —— 与任务书一致
- 6210_print 三个 3mf 解压确认都没有 .gcode
- **任务书没写的一条**：开机代码里先 `M620 S2A` 再 `T1000`（A1 专用"装料"占位），模型段第一个挤出之前没有 `T2`。
  只认 `T<n>` 会让第一段用错颜色，所以解析时 `M620 S<n>A` 也设定当前耗材，`T<n>`（n<16）再覆盖一次；T1000/T255 忽略。

## 任务 1＋2 解析、导出、判卷、逐层生长与换色（2026-09-14 完成）
- `index.html` 单页，双击或本地服务都能用；`vendor/` 里 three.js r159（最后一个带 classic script 构建的版本，
  file:// 下 ES module 会被 CORS 拦，所以没用新版）、mp4-muxer/webm-muxer 从 RoadTrip 抄。
- 3mf 用浏览器自带 DecompressionStream 解压；没有 `Metadata/plate_N.gcode` 就报"没切片"并给导出步骤。
- 挤出段用 InstancedBufferGeometry 一次上传，每帧只改 instanceCount；圆弧按 ≤0.6mm 展开。
  实测 bearing4c 展开后 471,542 段，tower 打开 0.6 秒、1080p30 10 秒导出 4.4 秒（无头 Chrome + Intel 核显）。
- 换色：`M620 S<n>A` 和 `T<n>`（n<16）都设当前耗材（原因见任务 0 复核）。热床外的挤出（吐料）丢掉。
- `verify.js` 走真实界面：CDP 往 `#file` 放文件、改控件、点导出、收浏览器下载，判定只用 ffprobe＋抽帧像素。

### 判卷时踩到的坑（都改进了判据）
1. "首帧到末帧像素变化 ≥0.2%"太松：cube 用例里横梁随 Z 升高就让 10% 像素变了。改成数"模型色（鲜艳）像素"的增量，
   同时把打印机里唯一带颜色的屏幕玻璃、蓝色补光改成中性色——改完空床 0 个鲜艳像素。
2. 下载目录里冒出两个 crx：Chrome 组件更新走了同一个下载目录。加 `--disable-component-update`，拒收检查只认页面发起的下载事件。
3. 反向验证 onecolor 第一次是**假红**：上一轮 Chrome 没退完、配置目录被锁，脚本自己崩了（退出码也是 1）。
   改成每轮独立配置目录、等 Chrome 退出；脚本自身出错改为退出码 2 并写明"不是判卷结果"。
4. `--break=flat`（只留第一层挤出）下"严格递增"照样过（0<3394<3559<3689<3751，横梁升高让第一层露得更多）。
   改成 25% 之后每档至少 +10%：正常 +38%/+29%/+23%，flat +5%/+4%/+2% → 变红。

### 反向验证（每组判据故意弄坏一次）
| 命令 | 结果 | 变红的判据 |
|---|---|---|
| `node verify.js --prove`（删光 E） | 退出 1 | 三个样本都被页面拒收"没找到任何挤出路径" |
| `--break=flat`（只留第一层） | 退出 1 | 生长判据 +5%/+4%/+2%；红、蓝占比不足 |
| `--break=onecolor`（颜色全改 #FF0000） | 退出 1 | 末帧蓝色 0.000% |
| `--break=samecam`（三机位同一个） | 退出 1 | 三对中间帧 0.0% 不同 |

## 任务 3 A1 场景、三机位、界面（2026-09-14 完成）
- 代码画的简化 A1：前后长的底座＋前右屏幕、左右立柱＋顶梁（门形框架）、两根 Z 丝杆、顶上料盘、横梁、喷头、送料管、右后吐料口。
  热床（连同模型）随 G-code 的 Y 前后动，横梁随 Z 升降，喷头随 X 走。没有用任何拓竹图片、STL、商标。
- 平滑延时：每帧吸附到层末，喷头停热床右侧、热床推到最前（实测不推前时环绕到背面会被横梁挡住模型）。
- 连续模式：按累计挤出长度均匀推进，喷头停在当前挤出点，热床跟着前后动（截图确认）。
- 三机位：内置相机固定斜视 / 环绕一圈 / 斜上俯视；"镜头远近"滑块在整机和模型特写之间插值，按画面宽高比自动取景。
- 补测判卷没覆盖的输入：纯 .gcode（颜色从 G-code 配置段读到）、两个盘的 .gcode.3mf（出现选盘下拉）——截图确认。
- tower 性能：打开 0.4 秒、1080p30 10 秒导出 4.3 秒，远低于 30 秒 / 5 分钟的门槛。

### 判卷冻结
`verify.js` sha256 = `2de107c62f9d2c44a8d14326dd443fa7162dc6a848205a6456902a88f15c796d`（此后改动须先写 BLOCKED.md）
冻结前最后一轮：`node verify.js` 退出 0（VERIFY PASS）；`--prove` / `--break=flat` / `--break=onecolor` / `--break=samecam` 全部退出 1，各自只打红对应判据。
