# BLOCKED — 待裁决清单

## 1. 冻结后改了 verify.js：`--url` 参数解析（2026-09-14，已改，请裁决是否认可）
- **原因**：任务书完成条件写的是 `node verify.js --url https://jyb635050-ai.github.io/printlapse/`（空格分隔）。
  冻结版只认 `--url=地址`，空格写法被静默忽略，脚本照样判本地文件并打印 VERIFY PASS——线上根本没被判，是假绿。
  证据：第一次线上判卷日志第二行是 `页面：http://localhost:8947/`。
- **改了什么**：参数解析同时认 `--url=地址` 和 `--url 地址`（`--break`、`--only` 同样处理）；`--url` 后面没给地址、
  或给了不认识的参数时退出码 2 并说明，不再静默忽略。
- **判据有没有变**：没有。所有期望值、阈值、用例一个没动；只改了"判哪个页面"的参数读取。
- **前后指纹**：冻结版 `2de107c62f9d2c44a8d14326dd443fa7162dc6a848205a6456902a88f15c796d` → 修正版见 PROGRESS.md。
- **反向验证**：见 PROGRESS.md「任务 4」：故意把 `--url` 指向一个没人监听的本机端口，必须 VERIFY FAIL。

## 2. 同一次修正里 verify.js 另外两处健壮性改动（判据同样没变）
- 页面打不开时（地址错、网站挂了）`load()` 返回"没打开"让各组判不过，拒收组不再对空元素取值——之前会让脚本自己崩（退出码 2），
  而判卷应当是退出码 1 的 VERIFY FAIL。反向验证：`--url http://127.0.0.1:1/` → 退出 1，6 项没过。
- 同时发现并修了**页面**（不是判卷）的一个问题：静态 HTML 里 `data-state="empty"`，远程加载 three.js 慢时判卷脚本（和手快的用户）
  会在监听挂上之前放文件，文件没人接。页面改成初始 `booting`、脚本就绪后才置 `empty`。
